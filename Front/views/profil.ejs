<%- include('./includes/header.ejs') %>
  <% console.log(locals); %>
    <main class="container">
      <div class="column">
        <h1>HELLO WOLRD</h1>
        <div class="column">
        </div>
        <br>
        <div class="album">
          <div id="form3">
            <% locals.post.success.reverse().map((post)=>{ %>
              <% if(post.userId===locals.me.id){ %>
                <div class="card-group">
                  <div class="card">
                    <% if (post.attachement !="" ){ %>
                      <img id="image" style=" border-radius: 10px; height: 200px;" src="/img/<%= post.attachement %>"
                        class="img-fluid H-50% W-50%" alt="...">
                      <% } else { %>
                        <img id="image" style=" border-radius: 10px; height: 200px;"
                          src="https://dummyimage.com/640x360/fff/aaa" class="img-fluid H-50% W-50%" alt="...">
                        <% } %>
                          <div class="card-body">
                            <h5 class="card-title courtprofil">
                              <%= post.texte %>
                            </h5>
                          </div>
                          <form action="/profil/put/<%= post.id %>" method="post" class="formKN">
                            <input type="text" name="texte" placeholder="modifier le texte" class="input1">
                            <br>
                            <br>
                            <input class="form-control form-control-sm" id="formFileSm " type="file" name="attachement">
                            <br>
                            <button type="submit" class="btn btn-primary btn-sm">Valider le nouveau post</button>
                            </a>
                          </form>
                  </div>
                </div>
                <% } %>
                  <% }) %>
          </div>
          <hr>
        </div>
      </div>
      </div>
      <br>
      <br>
      </div>
      </div>
    </main>
    </body>
    </html>